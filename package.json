{
  "name": "my-roguelike",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:web": "vite build",
    "build:desktop": "tauri build --no-bundle",
    "build:desktop:local": "tauri build --config src-tauri/tauri.local.conf.json",
    "build:desktop:release": "tauri build --config src-tauri/tauri.release.conf.json",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "pixi.js": "^8.19.0"
  }
}
